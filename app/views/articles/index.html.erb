<h1>Welcome to the Blog</h1>
<p>
<%= link_to "Create new article", new_article_path %>
</p>
<% @articles.each do |article| %>
	<div class='col-sm'>
	 	<h3><%= link_to article.title, article_path(article) %></h3><br>
	  	<p><%= article.description %></p><br>
	  	<%= link_to 'Edit', edit_article_path(article) %></td><t>
	  	<%= link_to 'Delete', article_path(article), method: :delete, data: {confirm: "Are you sure?"} %></t>
	 </div>
 <% end %>

